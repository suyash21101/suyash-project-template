generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  // Connection URL lives in prisma.config.ts (Prisma 7 moved it out of the schema).
  // Points at Aurora PostgreSQL via DATABASE_URL per env — see docs/AWS_INFRA_SETUP.md §14.
}

// Add your models here
