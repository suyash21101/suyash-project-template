# Default owner for everything
* @suyash21101

# Auth and security-sensitive code — always require review.
# Add concrete paths once the AWS-native auth lands (Cognito client, middleware,
# session helpers) — see docs/AWS_INFRA_SETUP.md §5.

# Database schema changes — always require review
prisma/ @suyash21101
sql/ @suyash21101

# CI/CD pipeline changes — always require review
.github/ @suyash21101
